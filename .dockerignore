# The backend image needs main.py, src/, scripts/ and data/processed/ only.
venv/
__pycache__/
*.pyc
.git/
.github/

# Frontend is deployed separately on Vercel.
frontend/
driver-cards/

# 6.7 GB locally, and ephemeral in the container anyway, it rebuilds on demand.
data/cache/

# data/locked_team.json is deliberately NOT ignored. Render's free tier has no
# persistent disk, so the lock has to ship inside the image to survive a redeploy.
# See .github/workflows/lock-team.yml.

notebooks/
results/
docs/
*.md
!README.md
.env
.env.local
