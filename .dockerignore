# The backend image needs main.py, src/, scripts/ and data/processed/ only.
venv/
__pycache__/
*.pyc
.git/
.github/

# Frontend is deployed separately on Vercel.
frontend/
driver-cards/

# 6.7 GB locally, and ephemeral in the container anyway, it rebuilds on demand.
data/cache/

# data/snapshots/ is deliberately NOT ignored. Render's free tier has no persistent
# disk, so the snapshots have to ship inside the image. See src/snapshots.py.

notebooks/
tests/
results/
docs/
*.md
!README.md
.env
.env.local
